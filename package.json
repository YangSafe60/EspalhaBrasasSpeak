{
  "name": "espalha-brasas",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "desktop": "npm --prefix apps/desktop run tauri",
    "desktop:dev": "npm --prefix apps/desktop run tauri -- dev",
    "desktop:build": "npm --prefix apps/desktop run build",
    "server": "cargo run -p speakapp-server",
    "server:release": "cargo build -p speakapp-server --release"
  }
}
