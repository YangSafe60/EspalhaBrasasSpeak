port: 7880
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50100
  use_external_ip: true
keys:
  # Fallback only. Compose overrides with --keys from LIVEKIT_API_KEY / LIVEKIT_API_SECRET
  # (GitHub secrets on the VPS, localhost defaults when unset). Secret >= 32 chars.
  devkey: espalha_brasas_dev_livekit_secret_32b
logging:
  level: info
room:
  auto_create: true
