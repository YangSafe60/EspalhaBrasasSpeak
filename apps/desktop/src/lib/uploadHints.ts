/** Shown next to image-only upload controls (avatar, emoji, server icon). */
export const IMAGE_UPLOAD_HINT =
  "Images are hosted on ImgBB — only the link is stored here.";

/** Composer / general attach hint (images + other files). */
export const ATTACH_UPLOAD_HINT =
  "Images → ImgBB. Other files → Litterbox (temporary, up to 72h). Only links are stored on the server.";

/** @deprecated Use IMAGE_UPLOAD_HINT or ATTACH_UPLOAD_HINT */
export const CATBOX_UPLOAD_HINT = ATTACH_UPLOAD_HINT;
