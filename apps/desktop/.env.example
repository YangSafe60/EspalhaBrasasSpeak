# Build-time API origin for the desktop app (not editable in the UI).
VITE_API_BASE=http://localhost:8080

# Optional: only needed if cargo isn't on PATH (desktop scripts auto-add ~/.cargo/bin).
# CARGO_HOME=C:\Users\You\.cargo
# CARGO_BIN=C:\Users\You\.cargo\bin
